import React from "react";
import { Layout } from "antd";
const { Content, Footer } = Layout;
const Footers = () => {
  return (
    <Footer
      style={{
        textAlign: "center"
      }}>
      abc@abc company
    </Footer>
  );
};

export default Footers;
